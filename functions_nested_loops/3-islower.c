#include "main.h"
/**
 * Islower - check for lowercase character
 * Description - check for lowercase character
 * Return: 1 or 0
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
	return (1);
	}
return (0);
}
